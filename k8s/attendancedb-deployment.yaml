apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: attendancedb-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendancedb
  labels:
    app: attendancedb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: attendancedb
  template:
    metadata:
      labels:
        app: attendancedb
    spec:
      containers:
      - name: attendancedb
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "root"
        - name: MYSQL_DATABASE
          value: "attendancedb"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        readinessProbe:
          exec:
            command:
            - mysqladmin
            - ping
            - -h
            - localhost
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        livenessProbe:
          exec:
            command:
            - mysqladmin
            - ping
            - -h
            - localhost
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 5
        volumeMounts:
        - name: attendancedb-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: attendancedb-storage
        persistentVolumeClaim:
          claimName: attendancedb-pvc