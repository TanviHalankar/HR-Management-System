apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: employeedb-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: employeedb
  labels:
    app: employeedb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: employeedb
  template:
    metadata:
      labels:
        app: employeedb
    spec:
      containers:
      - name: employeedb
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "root"
        - name: MYSQL_DATABASE
          value: "employeedb"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        readinessProbe:
          exec:
            command:
            - mysqladmin
            - ping
            - -h
            - localhost
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        livenessProbe:
          exec:
            command:
            - mysqladmin
            - ping
            - -h
            - localhost
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 5
        volumeMounts:
        - name: employeedb-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: employeedb-storage
        persistentVolumeClaim:
          claimName: employeedb-pvc